{
  "name": "checkout-service",
  "version": "0.1.0",
  "private": true,
  "description": "DEMO app — build fails until build-metrics-helper is added, making 'get the build green' a real task",
  "scripts": {
    "build": "node build.js",
    "start": "node index.js"
  }
}
